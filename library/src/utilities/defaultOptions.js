const defaultOptions = {
  series: [],
  axes: {},
  readout: {
    hoverMode: 'local',
  },
};

const defaultSeriesOptions = {
  xKey: 'x',
  yKey: 'y',
  xAxisPosition: 'bottom',
  yAxisPosition: 'left',
  isVisible: true,
  stroke: '',
  fill: '',
  className: '',
};

export { defaultOptions, defaultSeriesOptions };
